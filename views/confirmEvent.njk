   <script> 
            const formDelete = document.querySelector("#form-delete")
            formDelete.addEventListener("submit", function(event) {
                const confirmation = confirm('You want to delete this member ?')
                    if(!confirmation) {
                     event.preventDefault()
                    }
                })
    </script>